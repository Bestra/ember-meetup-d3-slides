
import { applyD3Scale } from 'd3-slides/helpers/apply-d3-scale';
import { module, test } from 'qunit';

module('Unit | Helper | apply d3 scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = applyD3Scale([42]);
  assert.ok(result);
});

